#include "main.h"

/**
 * _isdigit - mimics isdigit function and checks if a char is a digit
 * @c: char to be checked
 *
 * Return: 1 if digit, 0 if not.
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}

